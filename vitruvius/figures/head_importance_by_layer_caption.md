# Per-layer head-importance distribution

Box plot per (encoder, layer): distribution of Δ nDCG@10 across the heads in that layer, combined over the 3 BEIR subsets. Tests the intuition that early layers (surface features) are more prunable than late layers (task-specific).

Source: `experiments/phase7/head_importance/`.
