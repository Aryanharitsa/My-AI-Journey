# Head-importance heatmap — `bert-base`

Three panels, one per BEIR test subset. Each cell = Δ nDCG@10 when that (layer, head) is ablated (all others left intact). Redder = more essential. White stars mark top-5 most-important heads per panel. Color scale shared across panels for direct cross-dataset comparison.

Source: `experiments/phase7/head_importance/bert-base__*.json`.
